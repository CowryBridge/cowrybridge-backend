{
  "name": "cowrybridge-backend",
  "version": "0.1.0",
  "private": true,
  "description": "API layer for CowryBridge: indexes Stellar transactions, manages pool state, and serves the frontend.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^12.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
