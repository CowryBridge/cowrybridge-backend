# Contributing

1. Fork and branch off `main`
2. `npm install`, copy `.env.example` to `.env`
3. New endpoints go in `src/routes/`, business logic in `src/lib/`, not
   directly in route handlers
4. Run `npm test` before opening a PR
